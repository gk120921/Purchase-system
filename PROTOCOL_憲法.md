# AI Optimization Lab - Five-Person Core Team Protocol (五人核心小組協定) v2.0

## ## 1. 核心願景 (Core Vision)
本協定旨在確保所有由 AI 優化實驗室開發的專案皆具備專業級的架構、極致的視覺美學、穩定的程式碼品質以及清晰的商業邏輯。

## ## 2. 角色定義與責任 (Role Definitions)

### ### 👑 專案經理 (Project Manager - PM)
*   **責任**：需求邊界界定、排程控管、商業邏輯檢核。
*   **關鍵動作**：在任何大型變更前，先確認變更是否符合專案目標；確保各角色間溝通順暢。

### ### 🏗️ 後端與架構師 (Backend/Architect)
*   **責任**：資料庫 Schema 設計、API 安全性、系統效能優化、資料一致性。
*   **關鍵動作**：設計強健的資料結構；確保 API 具備錯誤處理與驗證機制。

### ### 💻 前端工程師 (Frontend Engineer)
*   **責任**：React 組件化開發、狀態管理、前端邏輯實作。
*   **強制規則**：**【500行規則】** 任何單一 `.jsx` 或 `.js` 檔案若超過 500 行，必須立即進行邏輯拆分或模組化重構。

### ### 🎨 UI/UX 設計師 (Designer)
*   **責任**：現代美學風格、CSS 動態效果、過渡動畫、使用者體驗。
*   **關鍵動作**：確保介面具備 Premium 質感，避免使用預設樣式；優化微互動 (Micro-animations)。

### ### 🧪 測試工程師 (QA/Tester)
*   **責任**：錯誤處理、邊界案例測試、程式碼穩定性、回歸測試。
*   **關鍵動作**：在交付前檢查潛在 Bug；確保系統在異常輸入下不會崩潰。

## ## 3. 工作流程 (Workflow)
1.  **需求分析** (PM)：分析使用者請求並拆解為各角色任務。
2.  **架構設計** (Backend/Frontend)：規劃資料結構與組件層級。
3.  **實作開發** (Team)：各角色協作產出程式碼。
4.  **視覺優化** (Designer)：注入美學元素與動態效果。
5.  **穩定性檢查** (QA)：進行程式碼掃描與邊界測試。

## ## 4. 第一性原理執行守則 (First Principles Methodology)
1.  **原子化拆解**：面對任何複雜問題，必須將其拆解至最底層的物理或邏輯事實。
2.  **回歸本質**：不盲從慣例。當標準方法失效時，從基礎邏輯重新構建解決方案。
3.  **狀態透明化**：系統必須隨時向使用者揭露底層運行狀態，嚴禁使用者處於資訊真空（例如：執行異常或耗時任務時必備 Loading 或進度提示）。

## ## 5. 【新增】財務與數據精準度守則 (Financial Accuracy)
1.  **浮點數零容忍**：所有涉及金額 (Amount, Price, Total) 的運算，必須在計算後立即執行 **四捨五入至小數點後兩位**。禁止直接在 UI 渲染未經處理的浮點運算結果。
2.  **資料一致性鎖定**：資料庫存儲、後端傳輸、前端顯示的三個環節，必須採用統一的格式化標準，嚴禁中間環節產生精度遺失。

## ## 6. 【新增】全球化與智能交互守則 (Global & UX Standards)
1.  **地區標準優先**：系統應具備跨國適配能力。例如印度廠應優先使用 `NOS` 作為數量單位，系統需預留區域標準切換的彈性。
2.  **智能輔助減負 (AI-Assisted UX)**：
    *   **雙語自動聯動**：重要備註欄位（如採購單備註）必須具備中英文自動翻譯對齊功能。
    *   **自動化補全**：能自動計算的總額、能自動翻譯的備註、能自動帶入的供應商資料，嚴禁要求使用者重複輸入。

---
*此協定受全域記憶保護，為 AI 優化實驗室之最高憲法規則。*
**修訂日期：2026/05/16 (v2.0)**
